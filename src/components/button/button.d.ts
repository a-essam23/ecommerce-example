import { BaseButtonProps, ThemeSizes, ThemeVariants } from "@types/components";

// export type ButtonTypes = "primary" | "secondary";

export interface ButtonProps
    extends BaseButtonProps,
        ThemeSizes,
        ThemeVariants {}
